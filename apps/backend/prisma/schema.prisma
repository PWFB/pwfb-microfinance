generator client {
  provider = "prisma-client-js"
  engineType = "client"
}

datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

// Existing schema retained; DailyCollection settlement fields added below.
