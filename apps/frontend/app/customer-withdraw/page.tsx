"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { useAuth } from "../../context/AuthContext";
import { pwfbApi } from "../../lib/pwfb-api";

type Wallet = { balance?: number; currency?: string };
type Institution = { id: string; name?: string; code?: string };

export default function CustomerWithdrawPage() {
  const router = useRouter();
  const { user, loading: authLoading } = useAuth();
  const [customerId, setCustomerId] = useState(""); const [customerName, setCustomerName] = useState(""); const [wallet, setWallet] = useState<Wallet | null>(null); const [institutions, setInstitutions] = useState<Institution[]>([]);
  const [destination, setDestination] = useState<"BANK" | "ATM">("BANK"); const [bankQuery, setBankQuery] = useState(""); const [bankId, setBankId] = useState(""); const [bankOpen, setBankOpen] = useState(false); const [accountNumber, setAccountNumber] = useState(""); const [accountName, setAccountName] = useState("");
  const [accountLookupLoading, setAccountLookupLoading] = useState(false); const [accountLookupMessage, setAccountLookupMessage] = useState(""); const [amount, setAmount] = useState(""); const [description, setDescription] = useState(""); const [loading, setLoading] = useState(true); const [submitting, setSubmitting] = useState(false); const [message, setMessage] = useState(""); const [success, setSuccess] = useState(false);

  useEffect(() => { if (authLoading) return; if (!user) { router.replace("/login"); return; } if (user.role !== "CUSTOMER") { router.replace("/dashboard"); return; } async function load() { try { const customer = await pwfbApi.customers.me(); if (!customer?.id) throw new Error("Customer account could not be found."); setCustomerId(customer.id); setCustomerName(String(customer.name || customer.fullName || customer.accountName || `${customer.firstName || ""} ${customer.lastName || ""}`).trim()); setWallet(await pwfbApi.banking.customerWallet(customer.id)); setInstitutions(await pwfbApi.banking.institutions() || []); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to load withdrawal details."); } finally { setLoading(false); } } load(); }, [authLoading, user, router]);

  useEffect(() => { if (destination !== "BANK" || !bankId || !/^\d{10}$/.test(accountNumber)) { setAccountName(""); setAccountLookupMessage(""); setAccountLookupLoading(false); return; } const bank = institutions.find((item) => item.id === bankId); if (!bank?.code) { setAccountLookupMessage("This bank has no provider bank code configured yet."); setAccountName(""); return; } let cancelled = false; setAccountLookupLoading(true); setAccountLookupMessage("Verifying account name…"); const timer = window.setTimeout(async () => { try { const result = await pwfbApi.banking.accountName(bank.code!, accountNumber); if (cancelled) return; const verifiedName = String(result?.accountName || result?.name || result?.data?.accountName || result?.data?.name || "").trim(); if (!verifiedName) { setAccountName(""); setAccountLookupMessage("Account name could not be verified. Check the bank and account number."); return; } setAccountName(verifiedName); setAccountLookupMessage("Account name verified by bank name enquiry."); } catch (error) { if (!cancelled) { setAccountName(""); setAccountLookupMessage(error instanceof Error ? error.message : "Unable to verify this account."); } } finally { if (!cancelled) setAccountLookupLoading(false); } }, 450); return () => { cancelled = true; window.clearTimeout(timer); }; }, [accountNumber, bankId, destination, institutions]);

  const balance = Number(wallet?.balance || 0); const currency = wallet?.currency || "NGN"; const money = (value: number) => `${currency === "NGN" ? "₦" : `${currency} `}${Number(value || 0).toLocaleString("en-NG", { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`; const filteredBanks = institutions.filter((bank) => `${bank.name || ""} ${bank.code || ""}`.toLowerCase().includes(bankQuery.toLowerCase())); const selectedBank = institutions.find((bank) => bank.id === bankId);

  async function submitWithdrawal() { setMessage(""); setSuccess(false); const numericAmount = Number(amount); if (!Number.isFinite(numericAmount) || numericAmount <= 0) { setMessage("Enter a valid withdrawal amount greater than zero."); return; } if (numericAmount > balance) { setMessage("Insufficient wallet balance."); return; } if (destination === "BANK") { if (!bankId) { setMessage("Select the bank account receiving the withdrawal."); return; } if (!selectedBank?.code) { setMessage("The selected bank has no provider bank code. Select another bank."); return; } if (!/^\d{10}$/.test(accountNumber.trim())) { setMessage("Enter a valid 10-digit destination account number."); return; } if (accountLookupLoading || !accountName.trim()) { setMessage("Wait for the bank account name to be verified before continuing."); return; } if (accountLookupMessage !== "Account name verified by bank name enquiry.") { setMessage("The destination account could not be verified. Check the bank and account number."); return; } if (customerName && accountName.trim().toLowerCase() !== customerName.toLowerCase()) { setMessage("Destination account name must match your PWFB account name before an automatic withdrawal can proceed."); return; } } if (!customerId) { setMessage("Customer account is not available."); return; } setSubmitting(true); try { const destinationText = destination === "ATM" ? "ATM withdrawal" : `${selectedBank?.name || "bank"} ${accountNumber}`; const providerBankCode = destination === "BANK" ? selectedBank?.code : undefined; const result = await pwfbApi.banking.withdraw(customerId, { amount: numericAmount, description: description.trim() || `Automatic ${destinationText} withdrawal`, destination, bankId: destination === "BANK" ? bankId : undefined, bankCode: providerBankCode, bank_code: providerBankCode, accountNumber: destination === "BANK" ? accountNumber : undefined, accountName: destination === "BANK" ? accountName.trim() : undefined }); setWallet(result?.wallet || wallet); setAmount(""); setDescription(""); setSuccess(true); setMessage("Withdrawal submitted successfully. The destination payment will be completed automatically after the payment provider confirms the transfer."); } catch (error) { setMessage(error instanceof Error ? error.message : "Withdrawal could not be completed."); } finally { setSubmitting(false); } }

  if (authLoading || loading) return <main className="min-h-screen bg-[#f4faf6] flex items-center justify-center"><p className="font-medium text-emerald-700">Loading withdrawal...</p></main>;
  return <main className="min-h-screen bg-[#f4faf6] pb-24"><div className="mx-auto w-full max-w-2xl px-4 pt-5"><Link href="/customer-dashboard" className="inline-flex items-center rounded-full bg-white px-3 py-2 text-sm font-semibold text-emerald-700 shadow-sm ring-1 ring-emerald-100">← Back</Link><header className="mt-5 mb-5"><p className="text-xs font-bold uppercase tracking-[0.18em] text-emerald-600">PWFB WALLET</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-900">Withdraw</h1><p className="mt-1 text-sm text-slate-500">Send your available wallet balance to your bank or ATM.</p></header><section className="overflow-hidden rounded-3xl bg-gradient-to-br from-emerald-700 via-emerald-600 to-green-500 p-6 text-white shadow-lg"><p className="text-xs font-bold uppercase tracking-[0.16em] text-emerald-100">Available Balance</p><p className="mt-3 text-4xl font-bold tracking-tight">{money(balance)}</p><p className="mt-2 text-sm text-emerald-100">Amount currently available to withdraw</p></section><section className="mt-5 rounded-3xl border border-slate-100 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-[0.14em] text-emerald-600">Withdrawal destination</p><h2 className="mt-1 text-xl font-bold text-slate-900">Where should we send it?</h2><div className="mt-4 grid grid-cols-2 gap-3"><button type="button" onClick={() => setDestination("BANK")} className={`rounded-2xl border p-4 text-left ${destination === "BANK" ? "border-emerald-500 bg-emerald-50 text-emerald-700" : "border-slate-200 bg-white text-slate-600"}`}><strong className="block">Bank account</strong><span className="mt-1 block text-xs">Send to your bank</span></button><button type="button" onClick={() => setDestination("ATM")} className={`rounded-2xl border p-4 text-left ${destination === "ATM" ? "border-orange-400 bg-orange-50 text-orange-700" : "border-slate-200 bg-white text-slate-600"}`}><strong className="block">ATM</strong><span className="mt-1 block text-xs">Cash withdrawal</span></button></div>{destination === "BANK" && <><label className="mt-5 block text-sm font-semibold text-slate-700">Bank</label><div className="relative mt-2"><button type="button" onClick={() => setBankOpen((open) => !open)} aria-expanded={bankOpen} className="flex w-full items-center justify-between rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3.5 text-left outline-none transition hover:bg-white focus:border-emerald-500 focus:bg-white focus:ring-4 focus:ring-emerald-50"><span className={selectedBank ? "font-medium text-slate-900" : "text-slate-400"}>{selectedBank?.name || "Select bank"}</span><span className={`ml-3 text-slate-500 transition-transform ${bankOpen ? "rotate-180" : ""}`}>⌄</span></button>{bankOpen && <div className="absolute left-0 right-0 top-full z-40 mt-2 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-xl"><div className="border-b border-slate-100 p-3"><input autoFocus value={bankQuery} onChange={(e) => setBankQuery(e.target.value)} placeholder="Search bank" className="w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm outline-none focus:border-emerald-500 focus:bg-white" /></div><div className="max-h-72 overflow-y-auto p-1">{filteredBanks.length ? filteredBanks.map((bank) => <button key={bank.id} type="button" onClick={() => { setBankId(bank.id); setBankQuery(""); setBankOpen(false); setAccountNumber(""); setAccountName(""); setAccountLookupMessage(""); }} className={`flex w-full items-center justify-between rounded-xl px-3 py-3 text-left text-sm ${bank.id === bankId ? "bg-emerald-50 font-bold text-emerald-700" : "text-slate-700 hover:bg-emerald-50"}`}><span>{bank.name || "Unnamed bank"}</span><span className="ml-3 shrink-0 text-xs text-slate-400">{bank.code || ""}</span>{bank.id === bankId && <span>✓</span>}</button>) : <p className="px-3 py-4 text-center text-sm text-slate-500">No matching bank found.</p>}</div></div>}</div><label className="mt-4 block text-sm font-semibold text-slate-700">Account number</label><input value={accountNumber} onChange={(e) => { setAccountNumber(e.target.value.replace(/\D/g, "").slice(0, 10)); setAccountName(""); setAccountLookupMessage(""); }} inputMode="numeric" placeholder="10-digit account number" className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3.5 outline-none focus:border-emerald-500 focus:bg-white" /><label className="mt-4 block text-sm font-semibold text-slate-700">Verified account name</label><input value={accountLookupLoading ? "Verifying account name…" : accountName} readOnly placeholder="Enter account number to verify" className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-100 px-4 py-3.5 font-semibold text-slate-800 outline-none" />{accountLookupMessage && <p className={`mt-2 text-xs ${accountLookupMessage === "Account name verified by bank name enquiry." ? "text-emerald-700" : "text-slate-500"}`}>{accountLookupMessage}</p>}<div className="mt-3 rounded-2xl bg-emerald-50 p-4 text-sm leading-6 text-emerald-800">PWFB verifies the destination account name before an automatic withdrawal. The verified name must match the customer's PWFB name.</div></>}{destination === "ATM" && <div className="mt-4 rounded-2xl bg-orange-50 p-4 text-sm leading-6 text-orange-800">ATM withdrawals require an enabled PWFB ATM/payment provider. The wallet will only be debited after the provider confirms the transfer.</div>}<label className="mt-5 block text-sm font-semibold text-slate-700">Amount</label><div className="mt-2 flex items-center rounded-2xl border border-slate-200 bg-slate-50 px-4 focus-within:border-emerald-500 focus-within:bg-white"><span className="text-lg font-bold text-emerald-600">₦</span><input type="number" min="1" max={balance} inputMode="decimal" value={amount} onChange={(e) => setAmount(e.target.value)} placeholder="0.00" className="w-full bg-transparent px-3 py-4 text-xl font-semibold outline-none" /></div><div className="mt-2 flex justify-between text-xs text-slate-500"><span>Available</span><span className="font-semibold text-emerald-700">{money(balance)}</span></div><label className="mt-4 block text-sm font-semibold text-slate-700">Description <span className="font-normal text-slate-400">(optional)</span></label><input value={description} onChange={(e) => setDescription(e.target.value)} placeholder="Withdrawal description" className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3.5 outline-none focus:border-emerald-500 focus:bg-white" /><button type="button" disabled={submitting} onClick={submitWithdrawal} className="mt-5 w-full rounded-2xl bg-emerald-600 px-4 py-4 font-bold text-white shadow-sm hover:bg-emerald-700 disabled:opacity-60">{submitting ? "Processing..." : "Continue Withdrawal"}</button>{message && <div className={`mt-4 rounded-2xl p-4 text-sm ${success ? "bg-emerald-50 text-emerald-700" : "bg-red-50 text-red-700"}`}>{message}</div>}</section></div><nav className="fixed bottom-0 left-0 right-0 z-50 border-t border-emerald-100 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-2xl items-center justify-around px-2 py-2"><Link href="/customer-dashboard" className="flex flex-col items-center px-2 py-1 text-slate-500"><span>⌂</span><span className="text-[10px]">Home</span></Link><Link href="/customer-deposit" className="flex flex-col items-center px-2 py-1 text-slate-500"><span>₦</span><span className="text-[10px]">Deposit</span></Link><Link href="/customer-withdraw" className="flex flex-col items-center px-2 py-1 text-emerald-600"><span>↗</span><span className="text-[10px] font-bold">Withdraw</span></Link><Link href="/customer-savings" className="flex flex-col items-center px-2 py-1 text-slate-500"><span>💰</span><span className="text-[10px]">Saving</span></Link><Link href="/customer-loans" className="flex flex-col items-center px-2 py-1 text-slate-500"><span>▣</span><span className="text-[10px]">Loan</span></Link><Link href="/customer-more" className="flex flex-col items-center px-2 py-1 text-slate-500"><span>•••</span><span className="text-[10px]">More</span></Link></div></nav></main>;
}
