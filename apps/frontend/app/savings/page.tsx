"use client";
import Link from "next/link";
import { useEffect,useMemo,useState } from "react";
import { apiRequest } from "../../lib/api";
type Savings={id:string;customerId:string;amount:number;accountType?:string;status?:string;createdAt?:string};
const money=(n:number)=>`₦${Number(n||0).toLocaleString("en-NG",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export default function SavingsPage(){
 const [items,setItems]=useState<Savings[]>([]),[loading,setLoading]=useState(true),[search,setSearch]=useState(""),[type,setType]=useState("all");
 useEffect(()=>{apiRequest("/savings").then(d=>setItems(Array.isArray(d)?d:d?.data??[])).catch(()=>setItems([])).finally(()=>setLoading(false))},[]);
 const total=items.reduce((s,x)=>s+Number(x.amount||0),0),active=items.filter(x=>(x.status||"ACTIVE").toUpperCase()==="ACTIVE").length;
 const filtered=useMemo(()=>items.filter(x=>{const q=search.toLowerCase().trim();return(!q||`${x.customerId} ${x.id} ${x.accountType||""}`.toLowerCase().includes(q))&&(type==="all"||String(x.accountType||"").toLowerCase()===type)}),[items,search,type]);
 return <main className="pwfb-module-page"><style>{`.pwfb-module-page{max-width:1280px;margin:auto;padding:8px 0 48px}.pwfb-module-hero{background:linear-gradient(135deg,#075b2a,#13813d);border-radius:24px;padding:28px;color:#fff;display:flex;justify-content:space-between;gap:24px;align-items:flex-end;margin-bottom:22px}.pwfb-module-hero h1{margin:5px 0;font-size:32px}.pwfb-module-hero p{margin:0;color:#d9eee0}.pwfb-module-hero .eyebrow{color:#ffb14b}.pwfb-hero-total{min-width:220px}.pwfb-hero-total small{display:block;color:#c9e3d1;font-weight:800;font-size:10px;letter-spacing:.12em}.pwfb-hero-total strong{display:block;font-size:30px;margin:5px 0}.pwfb-hero-total span{font-size:11px;color:#e4f3e8}.pwfb-metric-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-bottom:22px}.pwfb-metric{background:#fff;border:1px solid #e2ebe5;border-radius:18px;padding:20px}.pwfb-metric.orange{background:#fffaf4}.pwfb-metric span{font-size:11px;font-weight:800;color:#718078;text-transform:uppercase}.pwfb-metric strong{display:block;margin:7px 0;font-size:25px;color:#12351f}.pwfb-metric small{color:#7b877f}.pwfb-workspace{background:#fff;border:1px solid #e1eae4;border-radius:20px;overflow:hidden}.pwfb-workspace-head{padding:21px 22px;border-bottom:1px solid #e7eee9;display:flex;justify-content:space-between}.pwfb-workspace-head h2{margin:2px 0 4px;color:#103c22}.pwfb-workspace-head p{margin:0;color:#77847c;font-size:12px}.pwfb-eyebrow{font-size:10px;font-weight:900;letter-spacing:.13em;color:#13813d;margin:0}.pwfb-record-pill{background:#edf8f1;color:#176c39;border-radius:999px;padding:7px 11px;font-size:11px;font-weight:800;height:max-content}.pwfb-toolbar{padding:16px 22px;background:#f8fbf9;display:grid;grid-template-columns:minmax(0,1fr) 210px auto;gap:12px}.pwfb-field{display:flex;flex-direction:column;gap:6px}.pwfb-field span{font-size:10px;font-weight:800;color:#607067}.pwfb-field input,.pwfb-field select{height:45px;border:1px solid #d3dfd7;border-radius:11px;padding:0 12px;background:#fff}.pwfb-btn{display:inline-flex;align-items:center;justify-content:center;border-radius:11px;padding:0 16px;height:45px;font-weight:850;text-decoration:none;border:0}.pwfb-btn.primary{background:#0b7136;color:#fff}.pwfb-table-scroll{overflow:auto}.pwfb-table{width:100%;border-collapse:collapse;min-width:820px}.pwfb-table th{padding:13px 18px;background:#fbfcfb;color:#7a877f;text-align:left;font-size:10px;text-transform:uppercase}.pwfb-table td{padding:15px 18px;border-top:1px solid #edf1ee;font-size:12px;color:#33423a}.pwfb-customer{display:flex;align-items:center;gap:10px}.pwfb-avatar{width:38px;height:38px;border-radius:12px;background:#eaf7ef;color:#0b7136;display:grid;place-items:center;font-weight:900}.pwfb-customer strong,.pwfb-customer small{display:block}.pwfb-customer small{margin-top:3px;color:#8a958e;font-size:10px}.pwfb-amount{color:#0a6d35;font-size:13px}.pwfb-status{display:inline-flex;align-items:center;gap:6px;padding:6px 9px;border-radius:999px;background:#eaf8ef;color:#176b3b;font-size:10px;font-weight:850}.pwfb-status i{width:6px;height:6px;border-radius:50%;background:#24a148}.pwfb-actions{display:flex;gap:7px}.pwfb-action{padding:7px 10px;border-radius:8px;text-decoration:none;font-size:10px;font-weight:850}.pwfb-action.view{background:#edf8f1;color:#176b3b}.pwfb-action.edit{background:#fff1df;color:#a85b08}.pwfb-empty{padding:55px 20px;text-align:center;color:#718078}.pwfb-empty h3{margin:8px 0 4px;color:#173c25}.pwfb-empty .icon{margin:auto;width:52px;height:52px;border-radius:16px;background:#eaf7ef;color:#0b7136;display:grid;place-items:center;font-size:22px;font-weight:900}@media(max-width:760px){.pwfb-module-hero{padding:21px;flex-direction:column;align-items:flex-start}.pwfb-module-hero h1{font-size:27px}.pwfb-metric-grid{grid-template-columns:1fr}.pwfb-toolbar{grid-template-columns:1fr}.pwfb-workspace-head{padding:18px;flex-direction:column;gap:10px}.pwfb-toolbar{padding:14px 18px}}`}</style>
 <section className="pwfb-module-hero"><div><p className="eyebrow">SAVINGS OPERATIONS</p><h1>Savings Accounts</h1><p>Manage customer savings, deposits, balances and account records.</p></div><div className="pwfb-hero-total"><small>TOTAL SAVINGS VALUE</small><strong>{loading?"—":money(total)}</strong><span>{active} active account{active===1?"":"s"}</span></div></section>
 <section className="pwfb-metric-grid"><div className="pwfb-metric"><span>Total Accounts</span><strong>{loading?"—":items.length}</strong><small>Registered savings accounts</small></div><div className="pwfb-metric orange"><span>Total Savings</span><strong>{loading?"—":money(total)}</strong><small>Recorded savings value</small></div><div className="pwfb-metric"><span>Active Status</span><strong>{loading?"—":active}</strong><small>Accounts currently active</small></div></section>
 <section className="pwfb-workspace"><div className="pwfb-workspace-head"><div><p className="pwfb-eyebrow">ACCOUNT DIRECTORY</p><h2>Customer Savings Records</h2><p>Search, review and manage savings accounts.</p></div><span className="pwfb-record-pill">{loading?"Loading…":`${filtered.length} records`}</span></div><div className="pwfb-toolbar"><label className="pwfb-field"><span>Search customer or account</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Customer ID, account ID or type"/></label><label className="pwfb-field"><span>Account type</span><select value={type} onChange={e=>setType(e.target.value)}><option value="all">All account types</option>{Array.from(new Set(items.map(x=>x.accountType).filter(Boolean))).map(x=><option key={x} value={String(x).toLowerCase()}>{x}</option>)}</select></label><Link href="/savings/add" className="pwfb-btn primary">+ Add Savings</Link></div>{loading?<div className="pwfb-empty">Loading savings accounts…</div>:filtered.length===0?<div className="pwfb-empty"><div className="icon">₦</div><h3>No savings records found</h3><p>{items.length?"Try another search or account type.":"Start by adding the first customer savings account."}</p></div>:<div className="pwfb-table-scroll"><table className="pwfb-table"><thead><tr><th>Customer</th><th>Account / Reference</th><th>Deposit</th><th>Account Type</th><th>Status</th><th>Actions</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td><div className="pwfb-customer"><div className="pwfb-avatar">₦</div><div><strong>Customer</strong><small>{x.customerId}</small></div></div></td><td><strong>{x.id}</strong><small style={{display:"block",color:"#8a958e",marginTop:3}}>{x.createdAt?new Date(x.createdAt).toLocaleDateString("en-NG"):"Savings record"}</small></td><td><strong className="pwfb-amount">{money(x.amount)}</strong></td><td>{x.accountType||"Regular Savings"}</td><td><span className="pwfb-status"><i/>{x.status||"Active"}</span></td><td><div className="pwfb-actions"><Link href={`/savings/view/${x.id}`} className="pwfb-action view">View</Link><Link href={`/savings/edit/${x.id}`} className="pwfb-action edit">Edit</Link></div></td></tr>)}</tbody></table></div>}</section></main>;
}