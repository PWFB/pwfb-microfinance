"use client";

import Link from 'next/link';
import { useEffect, useMemo, useState } from 'react';
import { apiRequest } from '../../../lib/api';

type Row = { id: string; entryDate: string; description?: string; narration?: string; referenceNo?: string; totalAmount?: number; branch?: { name?: string }; [key: string]: any };
type Branch = { id: string; name: string };
const fields = [
  ['previousCashAtHand','Previous Cash at Hand'], ['dailyCashInHand','Daily Cash In-Hand'], ['savingsDeposits','Savings Deposits'], ['dailyInstallmentCollection','Daily Installment Collection'], ['monthlyCollection','Monthly Collection'], ['memberRegistration','Member Registration'], ['riskPremium','Risk Premium'], ['passbookSales','Passbook Sales'], ['loanDisbursement','Loan Disbursement'], ['bankDeposit','Bank Deposit'], ['savingsWithdrawal','Savings Withdrawal'], ['savingsReturnedAdjustment','Savings Returned/Adjustment'], ['fundTransfer','Fund Transfer'], ['other','Other'],
] as const;
const money=(v:any)=>new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN',maximumFractionDigits:0}).format(Number(v)||0);
export default function StaffWalletHistoryPage(){
 const [rows,setRows]=useState<Row[]>([]),[branches,setBranches]=useState<Branch[]>([]),[branchId,setBranchId]=useState(''),[from,setFrom]=useState(''),[to,setTo]=useState(''),[query,setQuery]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');
 async function load(){try{setLoading(true);setError('');const q=new URLSearchParams();if(branchId)q.set('branchId',branchId);if(from)q.set('from',from);if(to)q.set('to',`${to}T23:59:59`);const data=await apiRequest(`/cashbook/daily?${q}`);setRows(Array.isArray(data)?data:[])}catch(e:any){setError(e?.message||'Unable to load Cash Book history.')}finally{setLoading(false)}}
 useEffect(()=>{apiRequest('/branches').then(d=>setBranches(Array.isArray(d)?d:[])).catch(()=>setBranches([]))},[]);useEffect(()=>{void load()},[branchId,from,to]);
 const filtered=useMemo(()=>{const q=query.trim().toLowerCase();return q?rows.filter(r=>[r.description,r.narration,r.referenceNo,r.branch?.name].some(v=>String(v||'').toLowerCase().includes(q))):rows},[rows,query]);
 const totals=useMemo(()=>({receipts:filtered.reduce((s,r)=>s+fields.slice(0,8).reduce((x,[k])=>x+(Number(r[k])||0),0),0),payments:filtered.reduce((s,r)=>s+fields.slice(8).reduce((x,[k])=>x+(Number(r[k])||0),0),0)}),[filtered]);
 return <main className="min-h-screen bg-slate-50 p-4 md:p-8 text-slate-800"><div className="mx-auto max-w-7xl"><div className="mb-6 rounded-3xl bg-gradient-to-r from-emerald-800 to-emerald-600 p-6 text-white shadow-lg"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-black tracking-[.2em] text-orange-300">CREDIT OFFICER • FINANCE AUDIT</p><h1 className="text-3xl font-black">Cash Book History</h1><p className="mt-1 text-sm text-emerald-50">Review branch cash movement, verify collection activity and open any entry directly in the Cash Book.</p></div><div className="flex gap-2"><Link href="/cashbook" className="rounded-xl bg-orange-500 px-4 py-2 text-sm font-bold">Open Cash Book</Link><Link href="/dashboard" className="rounded-xl bg-white/10 px-4 py-2 text-sm font-bold">Dashboard</Link></div></div></div>
 <section className="mb-5 grid gap-4 md:grid-cols-3"><div className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-xs font-bold text-slate-500">RECORDS</p><strong className="mt-2 block text-2xl text-emerald-900">{filtered.length}</strong></div><div className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-xs font-bold text-slate-500">RECEIPTS / CASH IN</p><strong className="mt-2 block text-2xl text-emerald-700">{money(totals.receipts)}</strong></div><div className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-xs font-bold text-slate-500">PAYMENTS / CASH OUT</p><strong className="mt-2 block text-2xl text-orange-600">{money(totals.payments)}</strong></div></section>
 <section className="mb-5 rounded-2xl bg-white p-4 shadow-sm"><div className="grid gap-3 md:grid-cols-4"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search description, reference or branch" className="rounded-xl border border-slate-200 px-3 py-2 text-sm md:col-span-2"/><select value={branchId} onChange={e=>setBranchId(e.target.value)} className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="">All branches</option>{branches.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><div className="grid grid-cols-2 gap-2"><input type="date" value={from} onChange={e=>setFrom(e.target.value)} className="rounded-xl border border-slate-200 px-2 py-2 text-sm"/><input type="date" value={to} onChange={e=>setTo(e.target.value)} className="rounded-xl border border-slate-200 px-2 py-2 text-sm"/></div></div></section>
 {error&&<div className="mb-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}
 <section className="overflow-hidden rounded-2xl bg-white shadow-sm"><div className="border-b border-slate-100 p-4"><h2 className="font-black text-emerald-900">Branch Cash Ledger History</h2><p className="text-xs text-slate-500">Same records used by the Cash Book posting workflow. Credit Officers can review the ledger and open an entry for correction where their permissions allow.</p></div><div className="overflow-x-auto"><table className="min-w-[1150px] w-full text-sm"><thead className="bg-emerald-50 text-left text-xs uppercase tracking-wide text-emerald-900"><tr><th className="p-3">Date</th><th className="p-3">Branch</th><th className="p-3">Description</th><th className="p-3">Reference</th><th className="p-3 text-right">Cash In</th><th className="p-3 text-right">Cash Out</th><th className="p-3 text-right">Net</th><th className="p-3">Workflow</th></tr></thead><tbody>{filtered.map(r=>{const cashIn=fields.slice(0,8).reduce((s,[k])=>s+(Number(r[k])||0),0),cashOut=fields.slice(8).reduce((s,[k])=>s+(Number(r[k])||0),0);return <tr key={r.id} className="border-t border-slate-100 hover:bg-slate-50"><td className="p-3 whitespace-nowrap">{new Date(r.entryDate).toLocaleDateString('en-NG')}</td><td className="p-3">{r.branch?.name||'—'}</td><td className="p-3 font-medium">{r.description||r.narration||'—'}</td><td className="p-3">{r.referenceNo||'—'}</td><td className="p-3 text-right font-bold text-emerald-700">{money(cashIn)}</td><td className="p-3 text-right font-bold text-orange-600">{money(cashOut)}</td><td className={`p-3 text-right font-black ${cashIn-cashOut>=0?'text-emerald-700':'text-red-600'}`}>{money(cashIn-cashOut)}</td><td className="p-3"><Link href={`/cashbook?edit=${encodeURIComponent(r.id)}`} className="rounded-lg bg-emerald-700 px-3 py-1.5 text-xs font-bold text-white">Open / Edit</Link></td></tr>})}{loading&&<tr><td colSpan={8} className="p-10 text-center text-slate-500">Loading Cash Book history…</td></tr>}{!loading&&!filtered.length&&<tr><td colSpan={8} className="p-10 text-center text-slate-500">No Cash Book history found for the selected filters.</td></tr>}</tbody></table></div></section></div></main>
}
